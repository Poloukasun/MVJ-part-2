$(document).ready(() => {
    //     $("#search").on("input", () => {
    //         var inputValue = $("#search").val();

    //         var currentUrl = window.location.href;

    //         var regex = /(\?|&)q=.*?(&|$)/;

    //         if (currentUrl.match(regex)) {
    //             var updatedUrl = currentUrl.replace(regex, '$1q=' + encodeURIComponent(inputValue) + '$2');
    //         } else {
    //             var separator = currentUrl.includes('?') ? '&' : '?';
    //             var updatedUrl = currentUrl + separator + 'q=' + encodeURIComponent(inputValue);
    //         }


    //         window.history.replaceState({ path: updatedUrl }, '', updatedUrl);
    //     });
});
